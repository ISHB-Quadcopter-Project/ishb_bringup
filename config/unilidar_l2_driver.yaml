initialize_type: 2          # 1-Serial, 2-UDP
work_mode: 0                # std FOV, 3D, IMU on, Ethernet, auto-start
use_system_timestamp: true  # host clock, consistent with the IMU stamps
range_min: 0
range_max: 100
cloud_scan_num: 18

serial_port: "/dev/ttyACM0"
baudrate: 4000000

lidar_port: 6101
lidar_ip: "192.168.1.62"
pc_port: 6201
pc_ip: "192.168.1.2"        # the host NIC from Step 2

cloud_frame: "unilidar_lidar"
cloud_topic: "unilidar/cloud"
imu_frame: "unilidar_imu"
imu_topic: "unilidar/imu"

laserscan_frame: "unilidar_laserscan"
laserscan_topic: "unilidar/laserscan"
